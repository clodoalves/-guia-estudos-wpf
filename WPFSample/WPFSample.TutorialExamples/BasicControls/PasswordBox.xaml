<Window x:Class="WPFSample.TutorialExamples.BasicControls.PasswordBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFSample.TutorialExamples.BasicControls"
        mc:Ignorable="d"
        Title="PasswordBox" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="20" FontWeight="Bold">Basic example</TextBlock>
       
        <StackPanel Grid.Row="1" Margin="10">
            <Label>Text:</Label>
            <TextBox></TextBox>
            <Label>Password:</Label>
            <PasswordBox/>
        </StackPanel>

        <TextBlock Grid.Row="2" FontSize="20" FontWeight="Bold">Using PasswordChar</TextBlock>

        <StackPanel Grid.Row="3" Margin="10">
            <Label>Text:</Label>
            <TextBox></TextBox>
            <Label>Password:</Label>
            <PasswordBox PasswordChar="*" MaxLength="6"/>
        </StackPanel>

    </Grid>
</Window>
